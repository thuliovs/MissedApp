<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/radius_selector_container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:paddingTop="0dp"
    android:gravity="center">

    <ImageView
        android:id="@+id/compass_icon"
        android:layout_width="85dp"
        android:layout_height="85dp"
        android:src="@drawable/ic_compass"
        android:layout_marginTop="8dp"
        app:tint="@color/accent"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="in how many kilometers you wanna be waked?"
        android:textColor="@color/text_primary"
        android:textSize="15sp"
        android:fontFamily="@font/roboto_medium"
        android:layout_marginTop="12dp"
        android:textAlignment="center"/>

    <SeekBar
        android:id="@+id/radius_seekbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:layout_marginBottom="24dp"
        android:progressTint="@color/accent"
        android:thumbTint="@color/accent"
        android:progressBackgroundTint="@color/accent_tertiary"
        android:max="7"/>

    <TextView
        android:id="@+id/radius_value"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/text_primary"
        android:textSize="24sp"
        android:fontFamily="@font/roboto_medium"
        android:layout_marginBottom="24dp"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="8dp">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_confirm_radius"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="52dp"
            android:text="confirm"
            android:layout_marginEnd="4dp"
            android:textSize="15sp"
            android:fontFamily="@font/roboto_medium"
            android:textAllCaps="false"
            android:textColor="@color/background_dark"
            app:backgroundTint="@color/accent"
            app:cornerRadius="24dp"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_back_radius"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="52dp"
            android:text="cancel"
            android:layout_marginStart="4dp"
            android:textSize="15sp"
            android:fontFamily="@font/roboto_medium"
            android:textAllCaps="false"
            android:textColor="@color/text_primary"
            app:backgroundTint="@color/surface_dark"
            app:strokeColor="@color/accent"
            app:strokeWidth="1dp"
            app:cornerRadius="24dp"/>

    </LinearLayout>
</LinearLayout> 